# Bot: {{AGENT_NAME}}
# Generated: {{DATE}}

# Gateway authentication token (required)
OPENCLAW_GATEWAY_TOKEN={{GATEWAY_TOKEN}}

# Git identity for agent commits (workspace backups, etc.)
GIT_USER_NAME={{AGENT_NAME}}
GIT_USER_EMAIL={{AGENT_NAME}}@openclaw.local

# Anthropic API key (required for Claude models)
ANTHROPIC_API_KEY=

# Trusted proxy networks (OrbStack uses 192.168.x.x)
OPENCLAW_TRUSTED_PROXIES=192.168.0.0/16

# Docker-out-of-Docker port range (for dev containers)
BOTDADDY_DEV_PORT_START={{BOTDADDY_DEV_PORT_START}}
BOTDADDY_DEV_PORT_END={{BOTDADDY_DEV_PORT_END}}

# Web search (Brave Search API)
BRAVE_API_KEY=

# Mattermost (self-hosted)
# MATTERMOST_BOT_TOKEN=
# MATTERMOST_URL=https://mm.yourdomain.com

# Uncomment as needed:
# OPENAI_API_KEY=
